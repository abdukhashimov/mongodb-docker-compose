version: "3.7"

services:
    mongo:
        image: mongo:4.2
        restart: always
        volumes:
            - delever_mongodata:/data/db
        ports:
            - "27017:27017"

volumes:
    delever_mongodata:
